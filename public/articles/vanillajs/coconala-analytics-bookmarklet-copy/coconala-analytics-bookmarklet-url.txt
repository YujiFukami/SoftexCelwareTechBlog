javascript:(async()=>{try{const root=window.__NUXT__?.state?.pages?.analytics||window.$nuxt?.$store?.state?.pages?.analytics;if(!root)throw new Error("分析データが見つかりません。ココナラの分析ページで実行してください。");const service=root.serviceAnalytics;if(!service)throw new Error("サービス分析データが見つかりません。");const viewSaleList=service.numberOfTermsList||[];const impList=service.serviceImpressions?.numberOfTermsList||[];const viewSaleMap=new Map(viewSaleList.map(x=>[x.term,x]));const impMap=new Map(impList.map(x=>[x.term,x]));const dates=[...new Set([...viewSaleList.map(x=>x.term),...impList.map(x=>x.term)])].sort();const rows=dates.map(d=>{const vs=viewSaleMap.get(d)||{};const im=impMap.get(d)||{};return[d,im.impressionCount??"",vs.numberOfView??"",vs.numberOfSale??""];});const header=["日付","表示数","閲覧数","販売数"];const text=[header,...rows].map(r=>r.join("\t")).join("\n");try{await navigator.clipboard.writeText(text);}catch(e){const ta=document.createElement("textarea");ta.value=text;document.body.appendChild(ta);ta.select();document.execCommand("copy");ta.remove();}console.table(rows.map(r=>({"日付":r[0],"表示数":r[1],"閲覧数":r[2],"販売数":r[3]})));alert("ココナラ分析データをクリップボードにコピーしました。\n件数: "+rows.length+"日分\n\nExcelやスプレッドシートに貼り付けできます。");}catch(e){console.error(e);alert("取得に失敗しました。\n"+e.message);}})();